[build]
builder = "RAILPACK"
installCommand = "npm ci && npx playwright install chromium --with-deps"

[deploy]
startCommand = "node src/server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
